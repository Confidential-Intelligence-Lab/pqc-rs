# Stage 10B-6 migration review

Reviewer:
Date:
Repository commit:

For each open inventory row, assign:

- `public-structural`;
- `security-relevant`;
- `not-byte-data`;
- `false-positive`.

For security-relevant comparisons, record the selected primitive:

- `ct_eq_bytes`;
- `ct_eq_slices`;
- `ct_is_zero_bytes`;
- `ct_is_zero_slice`.
