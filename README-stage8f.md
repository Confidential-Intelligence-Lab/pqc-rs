# Stage 8F

Copy these files into the repository root. Resolve metadata placeholders, run `python3 scripts/check-release-metadata.py`, then `./scripts/run-stage8f-release-gate.sh`. Package with `./scripts/package-release-candidate.sh`. Do not publish until package contents and the external review plan are approved.
