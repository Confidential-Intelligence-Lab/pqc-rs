# Stage 9D-4A: ML-DSA Signing Preparation

This increment implements strict private-key decoding, the pure-ML-DSA
message representative, context-length enforcement, `rho_double_prime`
derivation, and `ExpandMask` sampling.

The rejection loop, challenge computation, norm checks, hints, and final
signature encoding are deferred to Stage 9D-4B.
