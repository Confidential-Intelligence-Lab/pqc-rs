# Stage 8A

Extract into the repository root, then run:

```bash
./scripts/install-security-tools.sh
./scripts/run-security-baseline.sh
```

This increment changes no cryptographic algorithm. It adds dependency policy,
CI security gates, documentation checks, and negative HPKE tests.
